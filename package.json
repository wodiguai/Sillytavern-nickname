{
  "name": "sillytavern-address-ledger",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check index.js && node --check core.js",
    "test": "node --test tests/*.test.mjs"
  }
}
